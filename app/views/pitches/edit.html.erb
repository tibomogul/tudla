<% content_for :title, "Editing pitch" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-2xl mb-4">Editing Pitch</h1>
  <%= render "form", pitch: @pitch %>
  <%= link_to "Back to pitch", pitch_path(@pitch), class: "btn btn-soft mt-4" %>
</div>
