<% content_for :title, "Settings - #{@organization.name}" %>

<div class="w-full">
  <h1 class="font-bold text-4xl mb-6">Settings</h1>

  <div class="card bg-base-100 shadow">
    <div class="card-body text-center text-base-content/60 py-12">
      <span class="iconify lucide--settings size-12 mx-auto mb-4 opacity-50"></span>
      <p>Organization settings coming soon.</p>
    </div>
  </div>
</div>
